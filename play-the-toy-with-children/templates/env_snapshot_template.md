# Environment Snapshot: [env tag]

One snapshot per environment; run ledger rows reference the tag. Make a new
snapshot when anything below changes. Results from environments without a
snapshot stay "preliminary" in claim wording.

- Env tag (referenced by `run_ledger.csv`): [env-a1b2c3]
- Snapshot date:

## Code

- Repository and commit hash:
- Uncommitted changes at run time (yes/no; if yes, why acceptable):

## Software

| package | version | pin source (lockfile / container / manual) |
|---|---|---|
| python |  |  |
|  |  |  |

## Hardware

- Machine / GPU:
- Relevant limits (memory, precision):

## Rebuild

- Command or container reference to reproduce this environment:
